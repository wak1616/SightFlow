// SightFlow Chrome Extension - Shared Constants
// Used across multiple content scripts

// Timing constants (in milliseconds)
const EXPANSION_DELAY = 150;    // Wait after expanding sections (Angular needs time to render)

